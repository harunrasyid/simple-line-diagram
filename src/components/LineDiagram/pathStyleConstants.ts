/**
 * Shared constants for route path styling (Deck.GL and Pixi).
 * Change these to adjust line width and dashed pattern in both renderers.
 */
export const ROUTE_LINE_WIDTH = 5;
export const DASH_LENGTH = 5;
export const GAP_LENGTH = 15;
